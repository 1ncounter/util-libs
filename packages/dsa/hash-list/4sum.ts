/**
 * @param {number[]} nums 数组
 * @param {number} target
 * @return {number[][]}
 */
export const fourSum = (numbers: number[], target: number): number[][] => {
  numbers.sort();

  return [];
};
